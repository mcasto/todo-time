{
	"name": "project-time",
	"version": "1.0.0",
	"description": "A companion tool to parse the data generated by the VS Code plugin TODO+ and return the total amount of time worked on that project",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"bin": {
		"project-time": "./bin/calculate-time.js"
	},
	"author": "Mike Casto",
	"license": "ISC",
	"dependencies": {
		"moment": "^2.27.0",
		"yargs": "^15.4.0"
	}
}
